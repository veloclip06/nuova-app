import { ScaffoldPlaceholder } from "@/components/scaffold-placeholder";

// Dashboard: card-paese con sigillo + prossime scadenze. Built in PROMPT 5.
export default function DashboardPage() {
  return (
    <ScaffoldPlaceholder
      eyebrow="Cruscotto"
      title="Il tuo quadro paese per paese"
      description="Card-paese con sigillo di stato, progresso configurazione e prossime scadenze. Costruito in PROMPT 5."
    />
  );
}
