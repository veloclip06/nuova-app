import { ScaffoldPlaceholder } from "@/components/scaffold-placeholder";

// Checker result + email gate — built in PROMPT 3.
export default function CheckResultPage() {
  return (
    <ScaffoldPlaceholder
      eyebrow="Risultato del check"
      title="Il tuo quadro EPR"
      description="Card per paese con sigillo di stato ed email gate. Costruito in PROMPT 3."
    />
  );
}
