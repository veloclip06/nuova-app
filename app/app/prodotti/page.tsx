import { ScaffoldPlaceholder } from "@/components/scaffold-placeholder";

// SKU + packaging components table, manual add + CSV import. Built in PROMPT 5.
export default function ProductsPage() {
  return (
    <ScaffoldPlaceholder
      eyebrow="Prodotti"
      title="SKU e imballaggi"
      description="Tabella prodotti con componenti di imballaggio, aggiunta manuale e import CSV."
    />
  );
}
